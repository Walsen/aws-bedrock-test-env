name: br-role
description: A program to create a role to grant access users to bedrock models.
runtime: python
#  name: python
#  options:
#    toolchain: uv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
